// Based on https://learnopengl.com/PBR/Theory

#ifdef GL_ES
precision highp float;
#endif

layout(push_constant, std430) uniform DrawCall {
  uint material_index;
}
draw_call;

layout(set = 1, binding = 0, std140) uniform SceneData {
  mat4 view_projection;
  vec3 cam_pos;
}
scene_data;

struct LightData {
  vec3 pos;
  float power;
};

layout(set = 1, binding = 1, std140) uniform Lights {
  LightData data[4];
}
lights;

struct MaterialData {
  vec3 albedo;
  float metallic;
  float roughness;
  float ao;
};

layout(set = 2, binding = 0, std430) buffer restrict readonly Materials {
  MaterialData data[];
}
materials;

layout(set = 2, binding = 1) uniform sampler2D albedo_map[32];
layout(set = 2, binding = 2) uniform sampler2D normal_map[32];
layout(set = 2, binding = 3) uniform sampler2D metallic_map[32];
layout(set = 2, binding = 4) uniform sampler2D roughness_map[32];
layout(set = 2, binding = 5) uniform sampler2D height_map[32];

layout(location = 0) in vec3 world_pos;
layout(location = 1) in vec3 normal;
layout(location = 2) in vec3 tangent;
layout(location = 3) in vec2 tex_coord;

layout(location = 0) out vec4 frag_color;

const float PI = 3.14159265359;

vec2 ParallaxOcclusionMapping(vec2 tex_coords, vec3 view_dir, uint mat_idx) {
  // Number of depth layers (quality/performance trade-off)
  const float num_layers = 32.0;
  float layer_depth = 1.0 / num_layers;

  // The amount to shift the texture coordinates per layer (this is the parallax
  // effect).
  float height_scale = 0.02;
  vec2 P = view_dir.xy * height_scale;
  vec2 delta_tex_coords = P / num_layers;

  // Start at the surface of the texture.
  vec2 current_tex_coords = tex_coords;
  float current_layer_depth = 0.0;

  // Step through layers until we pass the depth
  float current_height = texture(height_map[mat_idx], current_tex_coords).r;
  while (current_layer_depth < current_height) {
    // Move to the next layer.
    current_tex_coords -= delta_tex_coords;
    current_height = texture(height_map[mat_idx], current_tex_coords).r;
    current_layer_depth += layer_depth;
  }

  // -- Refinement Step --
  // After finding the intersection, we can refine it for better accuracy.
  // This performs a linear interpolation between the last two sample points.
  vec2 prev_tex_coords = current_tex_coords + delta_tex_coords;
  float after_height = current_height - current_layer_depth;
  float before_height = texture(height_map[mat_idx], prev_tex_coords).r -
                        current_layer_depth + layer_depth;

  float weight = after_height / (after_height - before_height);
  vec2 final_tex_coords =
      prev_tex_coords * weight + current_tex_coords * (1.0 - weight);
  return final_tex_coords;
}

float DistributionGGX(vec3 N, vec3 H, float roughness) {
  float a = roughness * roughness;
  float a2 = a * a;
  float NdotH = max(dot(N, H), 0.0);
  float NdotH2 = NdotH * NdotH;

  float nom = a2;
  float denom = (NdotH2 * (a2 - 1.0) + 1.0);
  denom = PI * denom * denom;

  return nom / denom;
}

float GeometrySchlickGGX(float NdotV, float roughness) {
  float r = (roughness + 1.0);
  float k = (r * r) / 8.0;

  float nom = NdotV;
  float denom = NdotV * (1.0 - k) + k;

  return nom / denom;
}

float GeometrySmith(vec3 N, vec3 V, vec3 L, float roughness) {
  float NdotV = max(dot(N, V), 0.0);
  float NdotL = max(dot(N, L), 0.0);
  float ggx2 = GeometrySchlickGGX(NdotV, roughness);
  float ggx1 = GeometrySchlickGGX(NdotL, roughness);

  return ggx1 * ggx2;
}

vec3 FresnelSchlick(float cosTheta, vec3 F0) {
  return F0 + (1.0 - F0) * pow(1.0 - cosTheta, 5.0);
}

void main() {
  uint mat_ind = draw_call.material_index;

  // Build TBN
  vec3 N = normalize(normal);
  vec3 T = normalize(tangent);
  vec3 B = normalize(cross(N, T));
  mat3 TBN = mat3(T, B, N);

  vec3 V = normalize(scene_data.cam_pos - world_pos);
  vec3 view_dir = normalize(TBN * V);

  // Parallax mapping to get displaced UVs
  vec2 parallax_tex_coords =
      ParallaxOcclusionMapping(tex_coord, view_dir, mat_ind);

  MaterialData material;

  material.albedo =
      pow(texture(albedo_map[mat_ind], parallax_tex_coords).rgb, vec3(2.2));
  material.metallic = texture(metallic_map[mat_ind], parallax_tex_coords).r;
  material.roughness = texture(roughness_map[mat_ind], parallax_tex_coords).r;
  material.ao = materials.data[mat_ind].ao;

  vec3 tangent_normal =
      texture(normal_map[mat_ind], parallax_tex_coords).xyz * 2.0 - 1.0;
  N = normalize(TBN * tangent_normal);

  // calculate reflectance at normal incidence; if dia-electric (like plastic)
  // use F0 of 0.04 and if it's a metal, use the albedo color as F0 (metallic
  // workflow)
  vec3 F0 = vec3(0.04);
  F0 = mix(F0, material.albedo, material.metallic);

  // reflectance equation
  vec3 Lo = vec3(0.0);
  for (int i = 0; i < 4; ++i) {
    // calculate per-light radiance
    vec3 L = normalize(lights.data[i].pos - world_pos);
    vec3 H = normalize(V + L);
    float distance = length(lights.data[i].pos - world_pos);
    float attenuation = 1.0 / (distance * distance);
    vec3 radiance = vec3(lights.data[i].power) * attenuation;

    // Cook-Torrance BRDF
    float NDF = DistributionGGX(N, H, material.roughness);
    float G = GeometrySmith(N, V, L, material.roughness);
    vec3 F = FresnelSchlick(max(dot(H, V), 0.0), F0);

    vec3 numerator = NDF * G * F;
    float denominator = 4.0 * max(dot(N, V), 0.0) * max(dot(N, L), 0.0) +
                        0.0001;  // + 0.0001 to prevent divide by zero
    vec3 brdf = numerator / denominator;

    // kS is equal to Fresnel
    vec3 kS = F;
    // for energy conservation, the diffuse and specular light can't
    // be above 1.0 (unless the surface emits light); to preserve this
    // relationship the diffuse component (kD) should equal 1.0 - kS.
    vec3 kD = vec3(1.0) - kS;
    // multiply kD by the inverse metalness such that only non-metals
    // have diffuse lighting, or a linear blend if partly metal (pure metals
    // have no diffuse light).
    kD *= 1.0 - material.metallic;

    // scale light by NdotL
    float NdotL = max(dot(N, L), 0.0);

    // add to outgoing radiance Lo
    Lo += (kD * material.albedo / PI + brdf) * radiance *
          NdotL;  // note that we already multiplied the BRDF by the Fresnel
                  // (kS) so we won't multiply by kS again
  }

  // ambient lighting
  vec3 ambient = vec3(0.03) * material.albedo * material.ao;
  vec3 color = ambient + Lo;

  // HDR tonemapping
  color = color / (color + vec3(1.0));
  // gamma correct
  color = pow(color, vec3(1.0 / 2.2));

  frag_color = vec4(color, 1.0);
}
