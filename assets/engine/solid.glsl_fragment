layout(push_constant) uniform Params {
  vec2 scale;
  vec2 offset;
  vec2 rotation;
  mat4 projection;
  vec4 color;
} params;

layout(location = 0) out vec4 frag_color;

void main() {
  frag_color = params.color;
}
