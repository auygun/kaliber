source_set("vma") {
  configs -= [ "//build:warnings" ]

  defines = [ "VMA_STATIC_VULKAN_FUNCTIONS=1" ]
  cflags = [ "-Wno-nullability-completeness" ]

  sources = [
    "vk_mem_alloc.cpp",
    "vk_mem_alloc.h",
  ]

  deps = []
}
